import { Action, Data, Payload, Options, Scanner, openCamera, capture } from '@shopify/app-bridge-core/actions/Scanner';
import type { ScannerAction } from '@shopify/app-bridge-core/actions/Scanner';
import { ClientApplication } from '../../client';
export { Action, Data, Payload, Options, Scanner, openCamera, capture, ScannerAction };
/**
 * @public
 */
export declare function create(app: ClientApplication, options?: Options): Scanner;
