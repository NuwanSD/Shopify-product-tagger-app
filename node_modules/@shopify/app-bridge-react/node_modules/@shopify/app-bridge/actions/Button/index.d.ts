import { Button, Options, Payload } from '@shopify/app-bridge-core/actions/Button';
import { ClientApplication } from '../../client';
export { Action, ButtonAction, ButtonUpdateAction, clickButton, ClickPayload, Icon, Presentation, Style, update, } from '@shopify/app-bridge-core/actions/Button';
export { Button, Options, Payload };
export declare function isValidButtonProps(button: Payload): boolean;
export declare function create(app: ClientApplication, options: Options): Button;
