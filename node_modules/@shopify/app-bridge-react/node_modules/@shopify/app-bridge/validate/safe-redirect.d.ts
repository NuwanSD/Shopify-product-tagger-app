import { Options, isSafe } from '@shopify/app-bridge-core/validate/safe-redirect';
export { Options, isSafe };
export declare function makeSafe(url: string, fallback: string, options?: Options): string;
