import type { ReactNode } from 'react';
export interface FilterButtonProps {
    onClick: () => void;
    'aria-label': string;
    children: ReactNode;
    disabled?: boolean;
}
export declare function FilterButton({ onClick, 'aria-label': ariaLabel, children, disabled, }: FilterButtonProps): JSX.Element;
//# sourceMappingURL=FilterButton.d.ts.map