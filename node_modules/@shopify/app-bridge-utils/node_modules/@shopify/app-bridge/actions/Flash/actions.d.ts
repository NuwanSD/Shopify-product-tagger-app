import { Toast, Options, ActionBase, clear, ClearAction, show, ShowAction, ToastAction } from '@shopify/app-bridge-core/actions/Toast';
import { ClientApplication } from '../../client';
export { ActionBase, clear, ClearAction, show, ShowAction, ToastAction as FlashAction };
export declare class Flash extends Toast {
}
export declare function create(app: ClientApplication, options: Options): Flash;
