import { Fullscreen, Action } from '@shopify/app-bridge-core/actions/Fullscreen';
import { ClientApplication } from '../../client';
export { Payload, FullscreenAction, enter, exit } from '@shopify/app-bridge-core/actions/Fullscreen';
export { Fullscreen, Action };
/**
 * Returns a new instance of a Fullscreen action set
 * @param app the client application
 *
 * @beta
 */
export declare function create(app: ClientApplication): Fullscreen;
